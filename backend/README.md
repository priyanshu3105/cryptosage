# Backend

## Setup
1. Copy `.env.example` to `.env` and fill values
2. Install deps: `npm install`
3. Run dev: `npm run dev`

## Notes
- MongoDB is Atlas (IP allowlist required)
- Config is in `src/config/*`